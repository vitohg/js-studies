let numeros = []

while (numeros.length <= 3) {
    let numero = prompt("Digite o numero dos array")

    numeros.push(numero)
}


document.write(`o numero das arrays: ${numeros}`)



/* let vh = ["vitor" ,"gabriel" ,"edson" ,"douglas"]

paraMaiuscula = function (x) {
    return x.toUpperCase()
}

// Bruxaria pura aqui
vh = vh.map(paraMaiuscula)

document.write(`${vh}`) */